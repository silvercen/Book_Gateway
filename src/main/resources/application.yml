eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 5962
spring:
  application:
    name: Book_Gateway
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: authormanagement
          uri: http://localhost:5960
          predicates:
            - Path=/authors/**
          filters:
            - AuthenticationFilter
        - id: bookmanagement
          uri: http://localhost:5959
          predicates:
            - Path=/bookapi/**
          filters:
            - AuthenticationFilter
        - id: Book_Authentication
          uri: http://localhost:5961
          predicates:
            - Path=/auth/**